{
  "name": "scathach-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cd ui && npm run dev",
    "build:ui": "cd ui && npm run build",
    "tauri": "tauri",
    "tauri:dev": "cargo tauri dev --manifest-path src-tauri/Cargo.toml",
    "tauri:build": "cargo tauri build --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "workspaces": [
    "ui"
  ]
}
