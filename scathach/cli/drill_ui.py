"""
Drill session UI.

Drill sessions are now handled by SessionRunner (same as quests) so they can be
persisted to the sessions table and resumed with `scathach session --resume <id>`.
This module is kept as a placeholder for any future drill-specific rendering helpers.
"""
